services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - .:/workspace
    ports:
      - "${APP_PORT_HOST}:8000"
    tty: true
    stdin_open: true
    platform: linux/amd64
