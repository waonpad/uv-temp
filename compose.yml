services:
  app:
    build:
      context: .
      dockerfile: ${APP_ENV:-dev}.Dockerfile
    volumes:
      - .:/workspace
    ports:
      - "${APP_PORT_HOST}:${APP_PORT}"
    tty: true
    stdin_open: true
