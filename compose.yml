services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
      target: ${APP_ENV:-development}
    volumes:
      - .:/workspace
    # ports:
    #   - "${APP_PORT_HOST}:${APP_PORT}"
    tty: true
    stdin_open: true
    command: /bin/bash
